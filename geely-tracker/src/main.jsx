import React from "react";
import ReactDOM from "react-dom/client";
import GeelyTracker from "./GeelyTracker";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GeelyTracker />
  </React.StrictMode>
);